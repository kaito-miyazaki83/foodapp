specVersion: "v1beta"
serviceId: "FoodApp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "foodapp-sql"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
